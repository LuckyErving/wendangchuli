Pillow>=10.0.0
img2pdf>=0.5.0
PyPDF2>=3.0.0
python-docx>=1.0.0
reportlab>=4.0.0

# Windows系统专用依赖
docx2pdf>=0.1.8; platform_system == "Windows"
pywin32>=300; platform_system == "Windows"

# 打包工具（可选）
pyinstaller>=6.0.0
